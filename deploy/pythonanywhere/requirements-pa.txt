# Subset of requirements.txt that works on PythonAnywhere's free / starter tier.
# WebSocket / Channels packages are kept (no harm) but no Daphne is launched —
# PA serves WSGI, so chat & realtime notifications won't push live updates.
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
djangorestframework-simplejwt==5.3.1
channels==4.1.0
Pillow==10.3.0
whitenoise==6.6.0
python-decouple==3.8

# Database driver — install only the one you use:
# mysqlclient==2.2.4         # for MySQL on PA
# psycopg2-binary==2.9.9     # for PostgreSQL (paid tier add-on)

# Optional dev tools (skip on PA to save quota):
# django-extensions==3.2.3
